<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.wanyj.auth.mapper.AuditLogMapper">

    <insert id="insert" parameterType="cn.wanyj.auth.entity.AuditLog">
        INSERT INTO audit_log (tenant_id, user_id, username, action, resource, detail, ip_address, created_at)
        VALUES (#{tenantId}, #{userId}, #{username}, #{action}, #{resource}, #{detail}, #{ipAddress}, #{createdAt})
    </insert>

</mapper>
